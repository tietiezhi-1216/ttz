# Changelog

## 2.2.0

- 统一 Codex、Grok、OpenCode Go 的简约账号列表、圆形已用额度和重置时间。
- 单胶囊跟随模型 provider；默认账号和明确账号槽位使用不同解析方式。
- Codex/Grok 设备码登录；OpenCode Go 密钥验证与保存。
- 同账号 OAuth 新凭据同步、自动续期、401 单次重试，避免展示失败账号的陈旧额度。
- 本地缓存轮询 60 秒；前台面板额度刷新 5 分钟；手动冷却 30 秒及并发去重。
- 修复多个胶囊实例同时弹出多层 Modal；关闭事件不再回传重开。
- 简化侧栏，管理操作收进更多菜单；切换前确认，导入前备份。
- 新增“检查更新”，通过 Paseo 的 Git 插件管理升级，不自下载执行代码。
- 补全 Git 安装依赖准备、锁定 OAuth 依赖、CI 检查、同事安装提示词。

### 注意

- 仅验证 Paseo 0.7.2 / Node.js 24 / macOS；其他平台的运行与权限保护尚未完整验证。
- 当前使用默认 Paseo/Pi 数据目录，运行 ID 必须为 `ttz`。
- 切换默认账号仍会重启主机上的 Paseo daemon。使用前保存重要任务。
- 凭据本机明文、0600 保存，不包含加密导出功能。
- 根包与插件包版本统一为 2.2.0；通过 GitHub Release 分发，不发布 npm 包。
